App As appinfo:
    BackEnabled: =true
    OnStart: |
        =ClearCollect(
            UniqueDetections,
            {Result: "Blank", Value: ""},  // Display as "Blank", store as empty
            Distinct(PPE, Detections)  // Unique detections
        )
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

